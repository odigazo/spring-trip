<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="battle">
   
   <insert id="insertBattleLog" parameterType="battleLog">
      INSERT INTO battleLog(winnerNum, loserNum, winnerName, loserName, battleField, winPoint, losePoint, bLog)
	  VALUES(#{winnerNum}, #{loserNum}, #{winnerName}, #{loserName}, #{battleField}, #{winPoint}, #{losePoint}, #{bLog})
   </insert>


</mapper>